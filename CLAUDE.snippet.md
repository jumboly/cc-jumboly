- 日本語で応答すること
- 使用するプログラミング言語やフレームワーク、ライブラリのベストプラクティスに従う
- コードコメントには「なぜそうするのか（Why）」を記載する（Whatではなく）
- ユーザーに選択肢を提示する際は、選びやすさのため各選択肢に項番（1./2./3. 等）を付与する

## 自作コマンド (cc-jumboly 提供)

- ユーザー作成のスラッシュコマンド/スキルは `j-` プレフィックスで識別する
  (組み込み command と区別しやすく、補完しやすくするため)。
- 定義は `~/.claude/commands/j-*.md` および `~/.claude/skills/j-*/SKILL.md`
  に配置される。発動条件と処理内容は各ファイル冒頭に記述されているのでそれに従う。
- `/j-handoff` と `/j-study` の使い分け：
  - パブリック知識を **claude.ai (Web) に持ち出す**前提 → `/j-handoff`
  - **Claude Code 内で完結**（プロプライエタリコード、社内事情、外に出せない
    題材）→ `/j-study`
  - 両者を示唆するフレーズが混在する場合は、起動前にユーザに確認する。
