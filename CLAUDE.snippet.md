## 自作コマンド (cc-jumboly 提供)

- ユーザー作成のスラッシュコマンド/スキルは `j-` プレフィックスで識別する
  (組み込み command と区別しやすく、補完しやすくするため)。
- 定義は `~/.claude/commands/j-*.md` および `~/.claude/skills/j-*/SKILL.md`
  に配置される。発動条件と処理内容は各ファイル冒頭に記述されているのでそれに従う。
