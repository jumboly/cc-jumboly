# MERGE.md — `~/.claude/` → repo 吸い上げ手順

このファイルは「（cc-jumboly に）ローカルから取り込んで」「マージして」と
依頼された Claude が読む手順書。リポジトリ root を CWD として実行する前提。

## ゴール

ローカル `~/.claude/` 配下にあるハーネス（手作業で追加・改良されたもの）を
このリポジトリに取り込み、単一情報源を最新に保つ。

## 手順

### 1. 候補列挙

スキャン対象（デフォルト）：

- `~/.claude/commands/j-*.md`
- `~/.claude/skills/j-*/**`

拡張対象（ファイルパスまたはディレクトリ名がユーザ発話に直接登場した場合のみ。
「全部」「ローカル全部」のような包括指示では拡張しない）：

- `~/.claude/hooks/*.sh`
- `~/.claude/settings.json` の特定キー
- `~/.claude/statusline-command.sh`
- その他

各候補について以下の状態を判定：

- **NEW**: リポジトリに対応物がない（ローカル独自）
- **CHANGED**: 両側にあり、内容が異なる
- **SAME**: 両側にあり、内容同一（無視）

### 2. 判断（候補ごと）

**NEW** には先頭数行サマリ + 「取り込む / スキップ / 名前を変えて追加」の
3 択を提示（diff は full body と同義なので省略）。

**CHANGED** には diff を提示し 4 択：

1. **取り込む** — local → repo 上書き
2. **スキップ** — ローカル独自として残す（リポジトリに入れない）
3. **逆方向に揃える** — リポジトリ側を採用してローカルを上書き
   （local の変更を破棄）。破壊的なので必ず確認。
4. **名前を変えて派生追加** — 例: `j-handoff` を `j-handoff-experimental` として
   別名でリポジトリに追加し、本家に影響させない

### 3. CLAUDE.md からの吸い上げ

1. `~/.claude/CLAUDE.md` を Read。
2. `<!-- BEGIN cc-jumboly -->` 〜 `<!-- END cc-jumboly -->` ブロック内に
   このリポジトリ `CLAUDE.snippet.md` と差異があれば、ユーザに提示し
   snippet 側を更新するか確認。
3. **マーカー外**にハーネス由来と思しきセクション（`j-` プレフィックス、
   `~/.claude/commands/` への言及など）があればユーザに提示し、
   `CLAUDE.snippet.md` への移管 + マーカー外からの削除を提案。
4. ユーザ普遍の好み（応答言語、コメント方針、レビュー態度など）は
   リポジトリに取り込まず CLAUDE.md 本体に残す。判断に迷ったらユーザに確認。

### 4. 書き込み + 報告

- 確認の取れた候補のみリポジトリ側に Write。
- 結果を要約：「N 件追加 / M 件更新 / K 件スキップ / L 件は逆方向で揃えた」
- `CLAUDE.snippet.md` の更新有無を併記。

### 5. コミットしない

- このフローでは `git commit` を勝手に行わない。
- 変更後の `git status` だけ提示し、コミットメッセージの提案はユーザに
  依頼されたときのみ行う。
